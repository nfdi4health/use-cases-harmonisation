on: 
  push:
    paths: 
      - rmonize/data_proc_elem/*

jobs:
  Copy_Folder:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Push to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.4
        env:
          API_TOKEN_GITHUB: ${{ secrets.HARMO_FIGURES }}
        with:
          source-files: 'rmonize/data_proc_elem/'
          destination-username: 'nfdi4health'
          destination-repository: 'harmonisation-results'
          destination-directory: 'rmonize'
          destination-branch: 'main'
          commit-username: 'FlorianSchw'
          commit-email: 'Florian.Schwarz@dife.de'
